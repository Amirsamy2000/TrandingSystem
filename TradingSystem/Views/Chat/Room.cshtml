@{
    ViewData["Title"] = "Community Chat";
    var communityId = ViewBag.CommunityId;
}

<style>

    #chat-messages {
        display: flex;
        flex-direction: column;
        padding: 10px;
    }

    .my-message {
        align-self: flex-end;
        background-color: #DCF8C6; /* WhatsApp-like */
        padding: 8px 12px;
        border-radius: 15px;
        margin: 4px 0;
        max-width: 70%;
    }

    .other-message {
        align-self: flex-start;
        background-color: #ECECEC;
        padding: 8px 12px;
        border-radius: 15px;
        margin: 4px 0;
        max-width: 70%;
    }



</style>


<div id="chat-messages" style="height:400px; overflow-y:auto; border:1px solid #ccc; margin-bottom:10px;"></div>
<div class="input-group mb-3">
    <input type="text" id="chat-input" class="form-control" placeholder="Type a message..." />
    <button class="btn btn-primary" onclick="sendMessage(document.getElementById('chat-input').value)">Send</button>
</div>
<script>
    const communityId = @communityId;
    const currentUserId = @ViewBag.UserId;
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
<script src="/js/chat.js"></script>
<script>
    function renderMessage(msg) {
        const container = document.getElementById('chat-messages');

        // áæ ÇáÑÓÇáÉ ãæÌæÏÉ ÞÈá ßÏå (ÈÇáÜ id Ãæ tempId) ãÊÖÝåÇÔ ÊÇäí
        if (msg.id && document.getElementById("msg-" + msg.id)) {
            return;
        }

        const div = document.createElement('div');
        div.id = "msg-" + msg.id; // ÇÓÊÎÏã id ááÑÓÇáÉ

        let time = '[Invalid Date]';
        if (msg.sentAt) {
            const d = new Date(msg.sentAt);
            if (!isNaN(d)) time = d.toLocaleTimeString();
        }

        const sender = msg.senderName || msg.userId || 'Unknown';
        const text = msg.text || '';

        div.textContent = `[${time}] ${sender}: ${text}`;

        if ((msg.userId ?? msg.senderId) == currentUserId) {
            div.classList.add("my-message");
        } else {
            div.classList.add("other-message");
        }

        container.appendChild(div);
        container.scrollTop = container.scrollHeight;
    }



    document.addEventListener('DOMContentLoaded', async function() {
        await start();
        // Load history
        fetch(`/Chat/History?communityId=${communityId}`)
            .then(res => res.json())
            .then(messages => {
                messages.forEach(renderMessage);
            });
    });
</script>
